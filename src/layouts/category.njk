<main class="category-main"> {# DEBUG #} <pre>{{ articles | dump }}</pre> {% if articles %} {% for article in articles %} <article class="category-article"> <div class="article-image"> <a href="{{ article.url }}"> <img src="{{ article.image or '/assets/images/news/default.jpg' }}" alt="{{ article.title }}"> </a> </div> <div class="article-content"> <h2 class="article-title"> <a href="{{ article.url }}">{{ article.title }}</a> </h2> <p class="date">{{ article.date }} | {{ article.tag or title }}</p> <p class="excerpt">{{ article.description }}</p> </div> </article> {% endfor %} {% else %} <p>No articles found for {{ title }}.</p> {% endif %} </main>